# Example env for e-learning-frontend
# Copy to .env and adjust values as needed

# Client API base URL (include path to API if necessary, e.g. https://api.example.com/api)
REACT_APP_API_BASE_URL=https://api.example.com/api

# Build mode used by scripts (optional: dev|production)
BUILD_MODE=dev

# NOTE: Do NOT store secret keys for payments or cloud services in the frontend repository.
# Payment keys (MoMo/ZaloPay), Cloudinary secrets, DB connection strings must be stored on the backend or in secure secret managers.
